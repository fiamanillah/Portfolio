info:
  name: Admin - Delete Experience
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/experiences/v1/admin/{{experienceId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
