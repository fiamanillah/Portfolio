info:
  name: Public - List Published Experiences
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/experiences/v1/public"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
