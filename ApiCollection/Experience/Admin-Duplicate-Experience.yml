info:
  name: Admin - Duplicate Experience
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/experiences/v1/admin/{{experienceId}}/duplicate"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
