info:
  name: Public - React To Post
  type: http
  seq: 25

http:
  method: POST
  url: "{{baseUrl}}/blogs/v1/public/slug/building-distributed-systems-websockets-redis/react"
  body:
    type: json
    data: |-
      {
        "reactionType": "like"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
