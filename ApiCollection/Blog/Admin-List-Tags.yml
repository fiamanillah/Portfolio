info:
  name: Admin - List Tags
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/blogs/v1/admin/tags"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
