info:
  name: Public - List Categories
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/blogs/v1/public/categories"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
