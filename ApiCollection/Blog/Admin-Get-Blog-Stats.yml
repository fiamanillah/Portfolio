info:
  name: Admin - Get Blog Stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/blogs/v1/admin/stats"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
