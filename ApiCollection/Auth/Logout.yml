info:
  name: Logout
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/auth/v1/logout"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: json
    data: |-
      {}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
