info:
  name: Demo Login (1-Click)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/v1/demo-login"
  body:
    type: json
    data: |-
      {
        "userId": "user-fi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
