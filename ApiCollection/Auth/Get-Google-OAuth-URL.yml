info:
  name: Get Google OAuth URL
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/auth/v1/google?returnTo=http://localhost:3000&mode=popup&format=json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
