info:
  name: Get Current User (Me)
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/auth/v1/me"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
