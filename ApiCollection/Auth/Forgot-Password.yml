info:
  name: Forgot Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/v1/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "fi@amanillah.dev"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
