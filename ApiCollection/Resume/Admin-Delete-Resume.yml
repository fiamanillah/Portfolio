info:
  name: Admin - Delete Resume Version
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/resume/v1/admin/{{resumeId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
