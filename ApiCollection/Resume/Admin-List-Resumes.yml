info:
  name: Admin - List All Resume Versions
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/resume/v1/admin?page=1&limit=10&sortBy=createdAt&sortOrder=desc"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
