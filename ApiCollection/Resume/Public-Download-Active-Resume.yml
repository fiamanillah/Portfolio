info:
  name: Public - Track & Download Active Resume
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/resume/v1/public/download"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
