info:
  name: Public - Get Active Resume Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/resume/v1/public/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
