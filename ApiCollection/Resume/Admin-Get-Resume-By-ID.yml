info:
  name: Admin - Get Resume Version Details
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/resume/v1/admin/{{resumeId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
