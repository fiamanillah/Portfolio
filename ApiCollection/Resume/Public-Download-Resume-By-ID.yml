info:
  name: Public - Track & Download Specific Resume Version
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/resume/v1/public/{{resumeId}}/download"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
