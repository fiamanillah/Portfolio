info:
  name: Public - List Skills
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/skills/v1/public"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
