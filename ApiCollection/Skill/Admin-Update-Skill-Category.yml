info:
  name: Admin - Update Skill Category
  type: http
  seq: 13

http:
  method: PATCH
  url: "{{baseUrl}}/skills/v1/admin/categories/{{categoryId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: json
    data: |-
      {
        "name": "Backend & Cloud Architecture (Updated)",
        "order": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
