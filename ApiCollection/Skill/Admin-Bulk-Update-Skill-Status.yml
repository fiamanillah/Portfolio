info:
  name: Admin - Bulk Update Skill Status
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/skills/v1/admin/bulk-status"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: json
    data: |-
      {
        "ids": [
          "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d"
        ],
        "status": "PUBLISHED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
