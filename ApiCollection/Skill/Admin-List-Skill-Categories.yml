info:
  name: Admin - List Skill Categories
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/skills/v1/admin/categories"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
