info:
  name: Admin - List Skills
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/skills/v1/admin/list?page=1&limit=20&status=PUBLISHED&sortBy=order&sortOrder=asc"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
