info:
  name: Admin - Seed Default Skills & Categories
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/skills/v1/admin/seed-default"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
