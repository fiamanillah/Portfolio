info:
  name: Admin - Delete Skill
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{baseUrl}}/skills/v1/admin/{{skillId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
