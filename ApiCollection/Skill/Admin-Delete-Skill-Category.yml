info:
  name: Admin - Delete Skill Category
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{baseUrl}}/skills/v1/admin/categories/{{categoryId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
