info:
  name: Admin - Get Moderation Stats
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/comments/v1/admin/stats"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
