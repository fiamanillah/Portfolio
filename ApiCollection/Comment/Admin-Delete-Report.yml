info:
  name: Admin - Delete Comment Report
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{baseUrl}}/comments/v1/admin/reports/{{reportId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
