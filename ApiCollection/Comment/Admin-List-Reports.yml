info:
  name: Admin - List Reports
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/comments/v1/admin/reports?page=1&limit=20&status=PENDING&sortBy=createdAt&sortOrder=desc"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
