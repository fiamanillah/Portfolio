info:
  name: Admin - Delete Newsletter Campaign
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/newsletter/v1/{{campaignId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
