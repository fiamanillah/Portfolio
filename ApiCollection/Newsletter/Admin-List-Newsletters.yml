info:
  name: Admin - List Newsletter Campaigns
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/newsletter/v1?page=1&limit=20&status=DRAFT&sortBy=createdAt&sortOrder=desc"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
