info:
  name: Admin - Send Test Newsletter
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/newsletter/v1/send-test"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: json
    data: |-
      {
        "campaignId": "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d",
        "testEmails": [
          "fi@amanillah.dev"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
