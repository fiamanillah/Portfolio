info:
  name: Delete Media File
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/media/v1/files/{{mediaId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
