info:
  name: Public Stream Media Asset
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/media/v1/stream/blogs/2026/architecture.png"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
