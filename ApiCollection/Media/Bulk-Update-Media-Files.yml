info:
  name: Bulk Update Media Files
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/media/v1/files/bulk-update"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: json
    data: |-
      {
        "ids": [
          "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d"
        ],
        "folder": "blogs",
        "tags": ["updated", "architecture"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
