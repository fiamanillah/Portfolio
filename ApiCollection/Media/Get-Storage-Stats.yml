info:
  name: Get Storage Stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/v1/stats"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
