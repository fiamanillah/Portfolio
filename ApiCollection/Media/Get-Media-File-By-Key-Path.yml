info:
  name: Get Media File By Key Path
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/media/v1/key/blogs/2026/architecture.png"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
