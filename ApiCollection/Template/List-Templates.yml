info:
  name: List Templates
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/templates/v1?page=1&limit=20&type=MARKETING"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
