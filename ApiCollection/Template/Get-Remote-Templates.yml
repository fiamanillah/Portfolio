info:
  name: Get Remote Templates
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/templates/v1/remote"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
