info:
  name: Get Template By ID Or Slug
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/templates/v1/weekly-digest"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
