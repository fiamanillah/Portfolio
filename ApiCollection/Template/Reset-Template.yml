info:
  name: Reset Template
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/templates/v1/TEMPLATE_ID/reset"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
