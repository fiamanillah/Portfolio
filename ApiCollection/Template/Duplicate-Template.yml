info:
  name: Duplicate Template
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/templates/v1/TEMPLATE_ID/duplicate"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
