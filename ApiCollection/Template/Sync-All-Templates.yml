info:
  name: Sync All Templates
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/templates/v1/sync"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
