info:
  name: Delete Profile Resume Document
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/users/v1/profile/resume"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
