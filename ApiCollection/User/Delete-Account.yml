info:
  name: Delete Account
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/users/v1/account"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
