info:
  name: Delete Profile Avatar (S3/R2)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/users/v1/profile/avatar"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
