info:
  name: Public - Get Resume Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/v1/public/resume"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
