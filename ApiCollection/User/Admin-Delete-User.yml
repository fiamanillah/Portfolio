info:
  name: Admin - Delete User
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/users/v1/admin/users/TARGET_USER_ID"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
