info:
  name: Admin - List Users
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/users/v1/admin/users?page=1&limit=20"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
