info:
  name: Admin - Export Subscribers
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/subscriber/v1/admin/export?status=subscribed"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
