info:
  name: Unsubscribe By Token
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/subscriber/v1/unsubscribe?token=sample_unsubscribe_token_123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
