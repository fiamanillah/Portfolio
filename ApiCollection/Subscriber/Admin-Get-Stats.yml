info:
  name: Admin - Get Subscriber Stats
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/subscriber/v1/admin/stats"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
