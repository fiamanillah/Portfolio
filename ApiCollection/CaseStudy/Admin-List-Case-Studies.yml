info:
  name: Admin - List Case Studies
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/case-studies/v1/admin/list?page=1&limit=10&sortBy=order&sortOrder=asc"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
