info:
  name: Admin - Delete Case Study
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/case-studies/v1/admin/{{caseStudyId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
