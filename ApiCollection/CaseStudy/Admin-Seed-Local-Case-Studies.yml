info:
  name: Admin - Seed Local Case Studies
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/case-studies/v1/admin/seed-local"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
