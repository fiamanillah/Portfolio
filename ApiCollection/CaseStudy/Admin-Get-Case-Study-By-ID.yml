info:
  name: Admin - Get Case Study By ID
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/case-studies/v1/admin/{{caseStudyId}}"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
