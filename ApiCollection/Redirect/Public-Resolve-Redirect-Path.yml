info:
  name: Public - Resolve Redirect Path
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/redirects/v1/resolve?path=/legacy-post-url"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
