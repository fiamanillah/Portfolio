info:
  name: Public - Google OAuth Redirect Callback
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/booking/v1/google/callback?code=4/0AbUR2VO_sample_code&state=secure_state_param"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
