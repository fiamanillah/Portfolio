info:
  name: Admin - Get Availability Schedule
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/booking/v1/admin/availability"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
