info:
  name: Admin - Get Google Calendar Status
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/booking/v1/admin/google/status"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
